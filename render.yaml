services:
  - type: web
    name: telegram-notification-bot
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node bot.js
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: BOT_USERNAME
        value: mrdanauthbot
      - key: FRONTEND_URL
        value: https://tdlib.vercel.app
      - key: PORT
        value: 3000
