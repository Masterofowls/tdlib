curl -X POST "https://api.telegram.org/bot$env:BOT_TOKEN/setWebhook" `
  -H "Content-Type: application/json" `
  -d '{\"url\": \"https://tdlib.vercel.app/api/webhook\"}'

vercel env add BOT_TOKEN
vercel env add BOT_USERNAME
vercel env add JWT_SECRET

# Set your actual bot token (the one that worked earlier)
$env:BOT_TOKEN = "8979661285:YOUR_COMPLETE_BOT_TOKEN_HERE"
